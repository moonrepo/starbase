[package]
name = "starbase_shell"
version = "0.0.1"
edition = "2021"
license = "MIT"
description = "Utilities for managing shell profiles."
repository = "https://github.com/moonrepo/starbase"

[package.metadata.docs.rs]
all-features = true

[dependencies]
starbase_styles = { version = "0.3.2", path = "../styles" }
miette = { workspace = true, optional = true }
thiserror = { workspace = true }

[features]
default = []
miette = ["dep:miette"]
